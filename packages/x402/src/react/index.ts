/**
 * X402 React Module
 *
 * React hooks for integrating X402 payments with wagmi wallet connections.
 * For non-React or custom wallet providers, use the `@cod3x/x402/client` module directly.
 *
 * @module react
 */
export { useX402Payment } from "./useX402Payment";
export type { UseX402PaymentReturn } from "./useX402Payment";
